package com.example.todolistapp

import android.view.View
import androidx.recyclerview.widget.RecyclerView

class TaskViewHolder(view: View) : RecyclerView.ViewHolder(view) {

    fun renderer(task: Task) {

    }
}